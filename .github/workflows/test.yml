name: Tests

on:
  push:

  workflow_dispatch:

jobs:
  testaction:
    runs-on: ubuntu-latest

    steps:
      - name: Run CustomPiOS
        id: test-action
        uses: KwadFan/run-custompios@main
        with:
          testaction: true
